# Isolated model-package validator runtime. Keep this separate from
# requirements-api.txt so the public control plane stays Torch-free.
# Do not regenerate requirements-macos-intel.lock.txt from this file.
torch==2.2.2
fastapi==0.115.12
pydantic==2.10.6
python-dotenv==1.0.1
python-multipart==0.0.20
uvicorn==0.34.0
